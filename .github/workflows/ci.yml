name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  java-build:
    name: Java (Maven + Spring Boot)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: java-manager

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Compile
        run: mvn compile -B --no-transfer-progress

      - name: Package
        if: success()
        run: mvn package -DskipTests -B --no-transfer-progress

  python-check:
    name: Python (依赖安装 + 语法检查)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: python-core

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Compile check (py_compile all .py files)
        run: python -m compileall -q engine/ api/ 2>/dev/null || python -m compileall engine/ api/

      - name: Verify critical imports
        run: |
          python -c "import fastapi; import uvicorn; import openai; import pydantic; print('Core OK')"
          python -c "import sentence_transformers; print('Embedding OK')"

  frontend-build:
    name: Frontend (Vite + React)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js 18
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build
